name: EduLibre Upload System

on:
  workflow_dispatch:
    inputs:
      tipo:
        required: true
      id:
        required: true
      titulo:
        required: true
      autor:
        required: true
      categoria:
        required: true
      descripcion:
        required: true
      archivo:
        required: false
      video:
        required: false
      audio:
        required: false
      imagen:
        required: false

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Run update script
        run: node scripts/update.js
        env:
          TIPO: ${{ inputs.tipo }}
          ID: ${{ inputs.id }}
          TITULO: ${{ inputs.titulo }}
          AUTOR: ${{ inputs.autor }}
          CATEGORIA: ${{ inputs.categoria }}
          DESCRIPCION: ${{ inputs.descripcion }}
          ARCHIVO: ${{ inputs.archivo }}
          VIDEO: ${{ inputs.video }}
          AUDIO: ${{ inputs.audio }}
          IMAGEN: ${{ inputs.imagen }}

      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "upload ${{ inputs.tipo }}" || echo "no changes"
          git push